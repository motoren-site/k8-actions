apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: app.ostepop.site
    http:
      paths:
      - path: /k8-actions
        pathType: Prefix
        backend:
          service:
            name: k8-actions-service
            port:
              number: 80